// external
import React from 'react';
import { observer } from 'mobx-react-lite';
import { Grid } from '@material-ui/core';

export const PostPresenter111 = observer(() => {
    return (
        <Grid container direction="row">
            122222222
        </Grid>
    );
});
